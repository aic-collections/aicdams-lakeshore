<%# TODO: I think this is dead code. Rights holder isn't used anywhere that I know of (awead) %>
<%= f.input :rights_holder_uris, as: :select, label: t('simple_form.labels.generic_work.rights_holder'),
    collection: [], selected: @form.rights_holder_uris,
    input_html: { class: 'form-control', multiple: true } %>
