<%= f.input key, as: :multi_value_select, collection: DocumentType.options, include_blank: true,
    selected: f.object.uris_for(:document_type),
    input_html: { class: 'form-control', multiple: true }, required: f.object.required?(key) %>
