<%= f.input :compositing_uri, as: :select, label: t('simple_form.labels.generic_file.compositing'),
	collection: Compositing.options, include_blank: true, selected: @form.uri_for(:compositing),
	input_html: { class: 'form-control', multiple: false } %>
